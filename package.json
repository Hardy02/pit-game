{
  "name": "pit-multiplayer",
  "version": "1.0.0",
  "description": "PIT — browser multiplayer PvP arena (zero-dependency Node WebSocket server)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
